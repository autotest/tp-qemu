- ppc_change_smt: install setup image_copy unattended_install.cdrom
    type = ppc_change_smt
    only ppc64 ppc64le
    smp = 8
    vcpu_maxcpus = ${smp}
    vcpu_sockets = 1
    vcpu_cores = 1
    vcpu_threads = ${smp}
