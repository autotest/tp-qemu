- balloon_small_size:
    virt_test_type = qemu
    no Win2000
    only Windows
    type = balloon_check
    balloon = balloon0
    balloon_dev_devid = balloon0
    balloon_dev_add_bus = yes
    ratio = 0.5
    small_size_check = yes
    test_tags = "evict"
    balloon_type = evict
    run_sub_test_after_balloon = yes
    sub_test_after_balloon = "boot"
    reboot_method = shell
    kill_vm_on_error = yes
    reboot = yes
    session_need_update = yes
    sleep_before_check = 90
    login_timeout = 900
