- edk2_stability_test:
    virt_test_type = qemu
    only Linux
    type = edk2_stability_test
    start_vm = no
    login_timeout = 120
    reboot_count = 20
    skip_image_processing = yes
    check_messgae = "enter to boot the selected OS"
