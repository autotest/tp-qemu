- test_vdpa_control_virtqueue:
    type = test_vdpa_control_virtqueue
    clone_master = yes
    master_images_clone = image1
    remove_image_image1 = yes
    only Linux
    only vdpa
    nettype_fixed = vdpa
    mem_fixed = 20480
    maxmem_mem = 25G
    slots_mem = 4
    size_mem = 1024M
    target_mem = mem0
    change_times = 65534
    change_cmd = "ip link set addr %s dev %s"
