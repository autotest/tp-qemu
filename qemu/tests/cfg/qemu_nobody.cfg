- qemu_nobody:
    start_vm = no
    user_runas = nobody
    type = qemu_nobody
    requires_root = yes
    qemu_sandbox_elevateprivileges = allow
    variants:
        - runas:
            required_qemu = (, 10.0.0)
        - run-with:
            required_qemu = [10.0.0,)

