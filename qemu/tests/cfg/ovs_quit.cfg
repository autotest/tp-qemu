- ovs_quit:
    type = ovs_quit
    image_snapshot = yes
    virt_test_type = qemu
    start_vm = no
    only no_pci_assignable
    nic_script = qemu-ifup
    nic_downscript = qemu-ifdown
    requires_root = yes
