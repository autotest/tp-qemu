- virtio_serial_stop_continue:
    virtio_ports = vc1
    virtio_port_type_vc1 = serialport
    type = stop_continue
    check_op = dd if=/dev/urandom of=/tmp/origin bs=1M count=100
    clean_op = rm -rf /tmp/origin
