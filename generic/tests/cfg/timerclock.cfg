- timerclock:
    only Linux
    virt_test_type = qemu
    variants:
        - monotonic_time:
            type = monotonic_time
        - rtc:
            type = rtc
        - tsc:
            type = tsc
