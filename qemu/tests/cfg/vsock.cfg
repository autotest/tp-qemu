- vsock_test:
    no Windows
    type = vsock_test
    vsocks = vhost_vsock0
    # TODO: Work out a python version nc-vsock and use the APIs directly
    variants:
        - @basic_test:
        - negative_test:
            type = vsock_negative_test
